{
  "name": "@molefas/trik-hash",
  "version": "1.0.2",
  "description": "Tool-mode trik for computing and comparing cryptographic hashes",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@trikhub/sdk": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}